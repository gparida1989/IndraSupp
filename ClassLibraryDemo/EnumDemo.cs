using System;
using System.Collections.Generic;
using System.Text;

namespace ClassLibraryDemo
{
    // it is a numeric constants
    // it is a datatype
    // value starts from 0 always, but u can ovverride
    // it is a value type,non-nullable

    public enum Weeks
    {
        Sunday,
        Monday,
        Tuesday,
        Wednesday,
        Thursday,
        Friday,
        Saturday
       
    }
}
